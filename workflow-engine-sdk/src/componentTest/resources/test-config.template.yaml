# Component test configuration — copy to test-config.yaml to override the defaults.
# Same shape as the TypeScript SDK's component test config.

workflowEngine:
  url: http://localhost:5503
  auth:
    type: token
    token: dev-token-123
    header: X-Kld-Authz
    scheme: "" # Empty for raw token, "Bearer" for Bearer token
